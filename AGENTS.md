# Repository Notes

- This checkout currently has no tracked commits and no source, README, manifest, CI, lint, test, formatter, or OpenCode config files to verify.
- Do not assume a language stack or developer commands until project files are added. Re-scan the root for manifests before implementing code.
- `.omo/` is local OpenCode session state, not project source or repo guidance.
- Update this file once the repository gains executable setup, commands, package boundaries, or workflow conventions.
